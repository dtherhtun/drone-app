apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hola-ing
spec:
  rules:
  - host: HOLA_DOMAIN
    http:
      paths:
      - path: /
        backend:
          service:
            name: hola-svc
            port:
              number: 80
